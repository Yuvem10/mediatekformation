{{form_start(form)}}
        
        {{ form_errors(form) }}

        {{form_label(form.name, null, {'label_attr':{'class':'form-label'}})}}
        {{form_widget(form.name, {'attr':{'class':'form-control'}})}}

        {{form_rest(form)}}
        <button type="submit" class="btn btn-primary" style="margin-top:20px">Ajouter une catégorie</button>

{{form_end(form)}}   